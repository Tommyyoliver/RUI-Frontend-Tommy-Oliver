import { UppercaseInput } from './uppercase-input';

describe('UppercaseInput', () => {
  it('should create an instance', () => {
    const directive = new UppercaseInput();
    expect(directive).toBeTruthy();
  });
});
